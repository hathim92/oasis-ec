import 'package:flutter/material.dart';

const fprimarycolor = Colors.white;
const double subheadingsize = 20;
const bigfamily = 'Poppins';
const smallfamily = 'OpenSans';
const defaultmargin = EdgeInsets.only(left: 80, right: 80);
const defaultpadding = EdgeInsets.only(top: 20, bottom: 20);
